export class Ingredient {
    /*this is a shortcut we created 2 propeties and when the class is created 
    it is going to automatically assign the values to the properties*/
    constructor(
        public name: string,
        public amount: number) {}
}
